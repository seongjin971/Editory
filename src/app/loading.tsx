export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--background)] px-5">
      <div className="rounded-lg border border-[var(--line)] bg-white px-5 py-4 text-sm font-semibold text-[#58615c]">
        불러오는 중
      </div>
    </main>
  );
}
